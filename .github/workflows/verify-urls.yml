# .github/workflows/verify-urls.yml

name: Verificación diaria de URLs de eventos

on:
  schedule:
    - cron: '0 5 * * *'
  workflow_dispatch:

jobs:
  verify-urls:
    runs-on: ubuntu-latest
    
    # ELIMINAMOS EL BLOQUE 'defaults' POR COMPLETO.
    # Todos los comandos se ejecutarán desde la raíz del proyecto.

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js v20
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Install dependencies with pnpm
        run: pnpm install --frozen-lockfile

      - name: Run URL verification script
        # Especificamos la ruta completa al script desde la raíz
        run: node ./apps/api/verify-event-urls.js
        env:
          # Usamos un nombre de variable consistente
          MONGODB_URI: ${{ secrets.MONGO_URI }}

      - name: Notify on failure
        if: failure()
        run: echo "🔴 ¡El workflow de verificación de URLs ha fallado!"